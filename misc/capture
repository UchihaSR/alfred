#!/usr/bin/env sh

# Stole from dylanaraps

scr_dir=~

date=$(date +%F)
time=$(date +%I-%M-%S)
file=$scr_dir/$date/$date-$time.png

mkdir -p "$scr_dir/$date"

ffmpeg -y \
    -hide_banner \
    -loglevel error \
    -f x11grab \
    -video_size "$(getresolution)" \
    -i :0.0 \
    -vframes 1 \
    "$file"

cp -f "$file" "$scr_dir/current.png"
